# =============================================================================
# requirements-cloud.txt — DataFlow E-Commerce Platform
# Cloud / prod dependencies (GKE workers, Airflow, Databricks)
#
# DO NOT install locally — only on cloud workers
# Install : pip install -r requirements-cloud.txt
# =============================================================================

# ── Airflow (Linux only — does NOT work on Windows natively) ──────────────────
apache-airflow==2.8.4
apache-airflow-providers-google==10.17.0
apache-airflow-providers-snowflake==5.3.1
apache-airflow-providers-slack==8.4.0
apache-airflow-providers-mongo==4.0.0

# ── dbt (install separately from pip) ────────────────────────────────────────
dbt-snowflake==1.7.4
# dbt-utils is a dbt package, NOT pip — install via: dbt deps
# see models/dataflow_ecommerce/packages.yml

# ── Data quality ──────────────────────────────────────────────────────────────
great-expectations==0.18.15

# ── MongoDB async (used by hourly sync job on GKE) ────────────────────────────
motor==3.4.0

# ── Snowflake extras ──────────────────────────────────────────────────────────
snowflake-sqlalchemy==1.5.3

# ── Observability ─────────────────────────────────────────────────────────────
datadog==0.49.1
opentelemetry-api==1.24.0
opentelemetry-sdk==1.24.0

# ── GCP extras ────────────────────────────────────────────────────────────────
google-cloud-secret-manager==2.20.0
